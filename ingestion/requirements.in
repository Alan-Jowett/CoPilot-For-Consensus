# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

# Ingestion Service Dependencies
# This file lists direct external dependencies with version ranges.
# Run `pip-compile requirements.in` to generate requirements.txt with pinned versions.
#
# Note: Local adapters (copilot-*) are installed separately in the Dockerfile
# via scripts/install_adapters.py and are not included in this lockfile.

python-dotenv>=1.2.1
prometheus-client>=0.24.1
fastapi>=0.127.0
# Note: uvicorn[standard] pulls in uvloop which does not support Windows.
# Use standard extras on non-Windows, and plain uvicorn on Windows.
uvicorn>=0.40.0; platform_system == "Windows"
uvicorn[standard]>=0.40.0; platform_system != "Windows"
httpx>=0.28.1  # Required for FastAPI TestClient
python-multipart>=0.0.20  # Required for file upload support
# Authentication
PyJWT>=2.10.1
cryptography>=46.0.3
