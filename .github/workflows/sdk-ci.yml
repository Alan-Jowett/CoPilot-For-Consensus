# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

name: SDK CI

on:
  push:
    branches: [ main ]
    paths:
      - 'sdk/**'
      - '.github/workflows/sdk-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'sdk/**'

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install SDK in development mode
      run: |
        python -m pip install --upgrade pip
        cd sdk
        pip install -e .
        pip install pytest pytest-cov

    - name: Run SDK tests
      run: |
        cd sdk
        pytest tests/ -v --tb=short --junit-xml=test-results.xml

    - name: Run draft_diff tests specifically
      run: |
        cd sdk
        pytest tests/test_draft_diff/ -v --tb=short

    - name: Generate coverage report
      run: |
        cd sdk
        pytest tests/ --cov=copilot_events --cov=draft_diff --cov-report=lcov --cov-report=html --cov-report=term

    - name: Upload coverage to Coveralls
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        path-to-lcov: ./sdk/coverage.lcov
        flag-name: python-${{ matrix.python-version }}
        parallel: true

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: sdk-test-results-${{ matrix.python-version }}
        path: sdk/test-results.xml

    - name: Upload coverage reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: sdk-coverage-report-${{ matrix.python-version }}
        path: sdk/htmlcov

    - name: Test Report
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: SDK Test Results (Python ${{ matrix.python-version }})
        path: sdk/test-results.xml
        reporter: 'java-junit'

  lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk
        python -m pip install --upgrade pip
        pip install -e .
        pip install pylint flake8

    - name: Run pylint on copilot_events
      run: |
        cd sdk
        pylint copilot_events/*.py --disable=C0111,C0103,R0903 || echo "Pylint warnings found but continuing..."

    - name: Run pylint on draft_diff
      run: |
        cd sdk
        pylint draft_diff/*.py --disable=C0111,C0103,R0903 || echo "Pylint warnings found but continuing..."

    - name: Run flake8
      run: |
        cd sdk
        flake8 copilot_events/*.py draft_diff/*.py tests/**/*.py --count --select=E9,F63,F7,F82 --show-source --statistics

  type-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk
        python -m pip install --upgrade pip
        pip install -e .
        pip install mypy types-pika

    - name: Run mypy on draft_diff
      run: |
        cd sdk
        mypy draft_diff --ignore-missing-imports || echo "Type checking completed with warnings"

  test-imports:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install SDK
      run: |
        python -m pip install --upgrade pip
        pip install ./sdk

    - name: Test copilot_events imports
      run: |
        python -c "
        from copilot_events import (
            EventPublisher,
            ArchiveIngestedEvent,
            ArchiveIngestionFailedEvent,
            create_publisher,
            RabbitMQPublisher,
            NoopPublisher
        )
        print('✓ copilot_events imports successful')
        "

    - name: Test draft_diff imports
      run: |
        python -c "
        from draft_diff import (
            DraftDiff,
            DraftDiffProvider,
            DatatrackerDiffProvider,
            MockDiffProvider,
            DiffProviderFactory,
            create_diff_provider
        )
        print('✓ draft_diff imports successful')
        "

    - name: Test draft_diff functionality
      run: |
        python -c "
        from draft_diff import create_diff_provider
        
        # Test mock provider
        provider = create_diff_provider('mock', {'default_format': 'text'})
        diff = provider.getdiff('draft-test', '01', '02')
        
        assert diff.draft_name == 'draft-test'
        assert diff.version_a == '01'
        assert diff.version_b == '02'
        assert diff.format == 'text'
        assert diff.source == 'mock'
        
        print('✓ draft_diff functionality test passed')
        "

  coveralls-finish:
    runs-on: ubuntu-latest
    needs: test
    if: always()
    steps:
    - name: Finalize Coveralls
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        parallel-finished: true

  summary:
    runs-on: ubuntu-latest
    needs: [test, lint, type-check, test-imports]
    if: always()
    
    steps:
    - name: Check test status
      run: |
        if [ "${{ needs.test.result }}" == "success" ]; then
          echo "✓ Tests passed"
        else
          echo "✗ Tests failed"
          exit 1
        fi
    
    - name: Report summary
      run: |
        echo "SDK CI Summary:"
        echo "- Tests: ${{ needs.test.result }}"
        echo "- Lint: ${{ needs.lint.result }}"
        echo "- Type Check: ${{ needs.type-check.result }}"
        echo "- Import Test: ${{ needs.test-imports.result }}"
