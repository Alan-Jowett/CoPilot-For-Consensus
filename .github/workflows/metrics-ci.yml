# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

name: Metrics Module CI

on:
  push:
    branches: [ main ]
    paths:
      - 'sdk/copilot_metrics/**'
      - '.github/workflows/metrics-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'sdk/copilot_metrics/**'
      - '.github/workflows/metrics-ci.yml'

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        cd sdk/copilot_metrics
        pip install -e .
        pip install pytest pytest-cov

    - name: Run unit tests
      run: |
        cd sdk/copilot_metrics
        pytest tests/ -v --tb=short --junit-xml=test-results.xml --cov=copilot_metrics --cov-report=lcov --cov-report=html --cov-report=term

    - name: Upload coverage to Coveralls
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        path-to-lcov: ./sdk/copilot_metrics/coverage.lcov
        flag-name: python-${{ matrix.python-version }}
        parallel: true

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: metrics-unit-test-results-${{ matrix.python-version }}
        path: sdk/copilot_metrics/test-results.xml

    - name: Upload coverage reports
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: metrics-unit-coverage-report-${{ matrix.python-version }}
        path: sdk/copilot_metrics/htmlcov

    - name: Test Report
      if: always()
      uses: dorny/test-reporter@v2
      with:
        name: Unit Test Results (Python ${{ matrix.python-version }})
        path: sdk/copilot_metrics/test-results.xml
        reporter: 'java-junit'

  lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk/copilot_metrics
        python -m pip install --upgrade pip
        pip install -e .
        pip install pylint flake8

    - name: Run pylint
      run: |
        cd sdk/copilot_metrics
        pylint copilot_metrics/ --disable=C0111,C0103,R0903 || echo "Pylint warnings found but continuing..."

    - name: Run flake8
      run: |
        cd sdk/copilot_metrics
        flake8 copilot_metrics/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics

  type-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk/copilot_metrics
        python -m pip install --upgrade pip
        pip install -e .
        pip install mypy

    - name: Run mypy
      run: |
        cd sdk/copilot_metrics
        mypy copilot_metrics/ --ignore-missing-imports || echo "Type checking warnings found but continuing..."

  test-imports:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk/copilot_metrics
        python -m pip install --upgrade pip
        pip install -e .

    - name: Test imports
      run: |
        cd sdk/copilot_metrics
        python -c "from copilot_metrics import *; print('✓ All imports successful')"

  security-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd sdk/copilot_metrics
        python -m pip install --upgrade pip
        pip install -e .
        pip install bandit safety

    - name: Run security check (bandit)
      run: |
        cd sdk/copilot_metrics
        bandit -r copilot_metrics/ -f json -o bandit-report.json
      continue-on-error: true

    - name: Upload Bandit report
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: metrics-bandit-report
        path: sdk/copilot_metrics/bandit-report.json

    - name: Run dependency security check (safety)
      run: |
        cd sdk/copilot_metrics
        safety check --json
      continue-on-error: true

  coveralls-finish:
    runs-on: ubuntu-latest
    needs: [unit-tests]
    if: always()
    steps:
    - name: Finalize Coveralls
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        parallel-finished: true

  summary:
    runs-on: ubuntu-latest
    needs: [unit-tests, lint, type-check, test-imports, security-check]
    if: always()
    
    steps:
    - name: Check unit tests status
      run: |
        if [ "${{ needs.unit-tests.result }}" == "success" ]; then
          echo "✓ Unit tests passed"
        else
          echo "✗ Unit tests failed"
          exit 1
        fi
    
    - name: Report summary
      run: |
        echo "Metrics Module CI Summary:"
        echo "- Unit Tests: ${{ needs.unit-tests.result }}"
        echo "- Lint: ${{ needs.lint.result }}"
        echo "- Type Check: ${{ needs.type-check.result }}"
        echo "- Import Tests: ${{ needs.test-imports.result }}"
        echo "- Security Check: ${{ needs.security-check.result }}"
