# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

name: Docker Compose CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository }}/

jobs:
  # Test docker-compose with cached images
  test-docker-compose:
    name: Test Docker Compose
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Free up disk space
      run: |
        echo "Disk space before cleanup:"
        df -h
        # Remove unnecessary software to free up ~10GB
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /opt/hostedtoolcache/CodeQL
        sudo docker system prune -af --volumes
        echo "Disk space after cleanup:"
        df -h

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Clean up any existing containers and volumes
      run: |
        # Stop and remove any existing containers and volumes from previous runs
        docker compose down -v || true
        # Also remove any dangling containers
        docker container prune -f || true

    - name: Validate docker-compose configuration
      run: docker compose config

    - name: Build services sequentially to avoid disk space issues
      run: |
        # Build services one at a time and prune between builds
        SERVICES="ingestion parsing chunking embedding orchestrator summarization reporting error-reporting"
        for service in $SERVICES; do
          echo "Building $service..."
          docker compose build $service
          # Prune dangling images to free space
          docker image prune -f
        done

    - name: Start services one at a time in dependency order
      run: |
        # Start services one at a time, waiting for each to become healthy
        # This makes it easy to see which service is failing
        
        # Helper function to start and wait for a service
        start_service() {
          local service=$1
          local timeout=$2
          
          echo ""
          echo "========================================="
          echo "Starting service: $service"
          echo "========================================="
          
          docker compose up -d "$service"
          
          # Check if service has a healthcheck
          has_healthcheck=$(docker compose ps "$service" --format "{{.Status}}" 2>/dev/null | grep -o "(health:" || echo "")
          
          if [ -z "$has_healthcheck" ]; then
            # Service has no healthcheck - it might be a one-shot validator
            # Wait a moment for it to start/complete
            sleep 5
            
            # Check exit code from container
            exit_code=$(docker compose ps "$service" --format "{{.ExitCode}}" 2>/dev/null || echo "")
            status=$(docker compose ps "$service" --format "{{.Status}}" 2>/dev/null || echo "")
            
            if [ "$exit_code" = "0" ]; then
              echo "✓ Service $service completed successfully (exit code 0)"
              docker compose logs "$service" --tail=20
              return 0
            elif echo "$status" | grep -q "Up"; then
              echo "✓ Service $service is running (no healthcheck)"
              return 0
            else
              echo "❌ Service $service failed (exit code: $exit_code, status: $status)"
              docker compose logs "$service" --tail=100
              exit 1
            fi
          fi
          
          # Service has a healthcheck, wait for it
          end=$((SECONDS + timeout))
          while [ $SECONDS -lt $end ]; do
            status=$(docker compose ps "$service" --format "{{.Status}}" 2>/dev/null || echo "")
            
            if echo "$status" | grep -q "(healthy)"; then
              echo "✓ Service $service is healthy"
              return 0
            fi
            
            if echo "$status" | grep -q "(unhealthy)"; then
              echo "❌ Service $service is unhealthy"
              docker compose logs "$service" --tail=100
              exit 1
            fi
            
            if echo "$status" | grep -q "Error"; then
              echo "❌ Service $service has error status: $status"
              docker compose logs "$service" --tail=100
              exit 1
            fi
            
            echo "Waiting for $service (status: $status)"
            sleep 3
          done
          
          echo "❌ Timeout waiting for $service to become healthy"
          docker compose logs "$service" --tail=100
          exit 1
        }
        
        # Start infrastructure services in order
        echo ""
        echo "========================================="
        echo "PHASE 1: Starting Infrastructure Services"
        echo "========================================="
        
        start_service "documentdb" 60
        start_service "messagebus" 60
        start_service "vectorstore" 60
        start_service "ollama" 60
        start_service "monitoring" 60
        start_service "pushgateway" 60
        start_service "loki" 30
        start_service "grafana" 90
        start_service "promtail" 60
        start_service "db-init" 30
        start_service "db-validate" 30
        start_service "vectorstore-validate" 60
        start_service "ollama-validate" 60
        
        # Start application services in order
        echo ""
        echo "========================================="
        echo "PHASE 2: Starting Application Services"
        echo "========================================="
        
        start_service "parsing" 120
        start_service "chunking" 120
        start_service "embedding" 120
        start_service "orchestrator" 120
        start_service "summarization" 120
        start_service "reporting" 120
        start_service "error-reporting" 120
        start_service "ingestion" 120
        
        echo ""
        echo "✓ All services started and healthy!"
        docker compose ps

    - name: Check service status
      run: docker compose ps

    - name: Check db-init and db-validate exit codes
      run: |
        set -euo pipefail
        for svc in db-init db-validate; do
          cid=$(docker compose ps -aq "$svc")
          if [ -z "$cid" ]; then
            echo "Container for $svc not found" >&2
            exit 1
          fi
          code=$(docker inspect "$cid" --format '{{.State.ExitCode}}')
          if [ "$code" != "0" ]; then
            echo "$svc failed with exit code $code" >&2
            docker compose logs "$svc"
            exit 1
          fi
        done

    - name: Run ingestion service
      run: |
        echo "Starting ingestion service..."
        docker compose run --rm ingestion

    - name: Check ingestion service exit code
      run: |
        echo "Verifying ingestion completed successfully..."
        # The previous step will fail if exit code is non-zero
        echo "✓ Ingestion service completed successfully"

    - name: View ingestion logs
      if: always()
      run: docker compose logs ingestion

    - name: Test reporting service health endpoint
      run: |
        echo "Testing reporting service..."
        max_attempts=10
        attempt=1
        while [ $attempt -le $max_attempts ]; do
          if curl -f http://localhost:8080/ 2>/dev/null; then
            echo "✓ Reporting service is healthy"
            exit 0
          fi
          echo "Attempt $attempt/$max_attempts: Reporting service not responding yet..."
          sleep 3
          attempt=$((attempt + 1))
        done
        echo "❌ Reporting service failed to respond after $max_attempts attempts"
        docker compose logs reporting --tail=50
        exit 1

    - name: Test reporting API endpoint
      run: |
        echo "Testing reports API..."
        curl -f http://localhost:8080/api/reports || (docker compose logs reporting --tail=50 && exit 1)

    - name: Test error-reporting service health endpoint
      run: |
        echo "Testing error-reporting service..."
        max_attempts=10
        attempt=1
        while [ $attempt -le $max_attempts ]; do
          if curl -f http://localhost:8081/ 2>/dev/null; then
            echo "✓ Error-reporting service is healthy"
            exit 0
          fi
          echo "Attempt $attempt/$max_attempts: Error-reporting service not responding yet..."
          sleep 3
          attempt=$((attempt + 1))
        done
        echo "❌ Error-reporting service failed to respond after $max_attempts attempts"
        docker compose logs error-reporting --tail=50
        exit 1

    - name: Test error-reporting UI endpoint
      run: |
        echo "Testing error-reporting UI..."
        curl -f http://localhost:8081/ui || (docker compose logs error-reporting --tail=50 && exit 1)

    - name: Test error-reporting API
      run: |
        echo "Testing error-reporting stats API..."
        curl -f http://localhost:8081/api/stats || (docker compose logs error-reporting --tail=50 && exit 1)

    - name: Test Grafana health endpoint
      run: |
        echo "Testing Grafana..."
        curl -f http://localhost:3000/api/health || exit 1

    - name: Test Prometheus health endpoint
      run: |
        echo "Testing Prometheus..."
        curl -f http://localhost:9090/-/healthy || exit 1

    - name: Test Loki health endpoint
      run: |
        echo "Testing Loki..."
        max_attempts=30
        attempt=1
        while [ $attempt -le $max_attempts ]; do
          if curl -f http://localhost:3100/ready 2>/dev/null; then
            echo "Loki is ready"
            exit 0
          fi
          echo "Attempt $attempt/$max_attempts: Loki not ready yet, waiting..."
          sleep 5
          attempt=$((attempt + 1))
        done
        echo "Loki failed to become ready after $max_attempts attempts"
        exit 1

    - name: View service logs
      if: always()
      run: docker compose logs --tail=50

    - name: Stop all services
      if: always()
      run: docker compose down

    - name: Verify clean shutdown
      if: always()
      run: |
        echo "Verifying all containers stopped..."
        if [ "$(docker compose ps -q | wc -l)" -eq 0 ]; then
          echo "✓ All services stopped successfully"
        else
          echo "✗ Some services failed to stop"
          docker compose ps
          exit 1
        fi
