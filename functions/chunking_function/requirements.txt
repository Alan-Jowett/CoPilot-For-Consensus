# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

# Azure Functions Python requirements
# These are the dependencies specific to Azure Functions runtime

# Azure Functions runtime
azure-functions>=1.18.0,<2.0.0

# Core adapters from parent project
# Note: In production, these would be installed from the parent project
# For POC, we'll reference the parent directory packages

# Service Bus client for publishing events
azure-servicebus>=7.11.0,<8.0.0

# Document store
pymongo>=4.6.0,<5.0.0

# Message bus (for publishing events)
pika>=1.3.0,<2.0.0

# Logging
python-json-logger>=2.0.0,<3.0.0

# Application Insights
opencensus-ext-azure>=1.1.0,<2.0.0
opencensus-ext-logging>=0.1.0,<1.0.0

# Schema validation
jsonschema>=4.17.0,<5.0.0

# Metrics (optional - can push to Application Insights)
prometheus-client>=0.17.0,<1.0.0
