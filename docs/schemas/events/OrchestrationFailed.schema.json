{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alan-jowett.github.io/CoPilot-For-Consensus/schemas/events/orchestration-failed.schema.json",
  "title": "OrchestrationFailed Event",
  "allOf": [
    { "$ref": "./event-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "event_type": { "const": "OrchestrationFailed" },
        "data": {
          "type": "object",
          "properties": {
            "thread_ids": {
              "type": "array",
              "items": { "type": "string", "pattern": "^[0-9a-f]{16,64}$" },
              "minItems": 1,
              "uniqueItems": true
            },
            "error_type": { "type": "string", "minLength": 1 },
            "error_message": { "type": "string", "minLength": 1 },
            "retry_count": { "type": "integer", "minimum": 0 }
          },
          "required": [
            "thread_ids",
            "error_type",
            "error_message",
            "retry_count"
          ],
          "additionalProperties": false
        }
      },
      "required": ["event_type", "event_id", "timestamp", "version", "data"],
      "additionalProperties": false
    }
  ]
}
