{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mock Draft Diff Provider Configuration",
  "description": "Configuration for mock draft diff provider (testing)",
  "type": "object",
  "properties": {
    "default_format": {
      "type": "string",
      "enum": ["html", "text", "markdown"],
      "default": "text",
      "description": "Default format for auto-generated mock diffs"
    },
    "mock_diffs": {
      "type": "object",
      "description": "Predefined mock diffs as a mapping (not fully schematized here)",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
