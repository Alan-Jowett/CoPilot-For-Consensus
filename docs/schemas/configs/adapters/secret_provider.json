{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$comment": "SPDX-License-Identifier: MIT\nCopyright (c) 2025 Copilot-for-Consensus contributors",
    "title": "Secret provider adapter",
    "type": "object",
    "properties": {
        "discriminant": {
            "field": "secret_provider_type",
            "type": "string",
            "source": "env",
            "env_var": "SECRET_PROVIDER_TYPE",
            "enum": ["local", "azure", "azurekeyvault"],
            "description": "Secret provider backend type (local, azure, azurekeyvault)"
        },
        "drivers": {
            "type": "object",
            "properties": {
                "local": {"$ref": "./drivers/secret_provider/secret_local.json"},
                "azure": {"$ref": "./drivers/secret_provider/azurekeyvault.json"},
                "azurekeyvault": {"$ref": "./drivers/secret_provider/azurekeyvault.json"}
            },
            "additionalProperties": false
        }
    },
    "required": ["discriminant", "drivers"]
}