# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Copilot-for-Consensus contributors

# Auth Service Dependencies
# This file lists direct external dependencies with version ranges.
# Run `pip-compile requirements.in` to generate requirements.txt with pinned versions.
#
# Note: Local adapters (copilot-*) are installed separately and not included in this file.
# See Dockerfile for adapter installation via scripts/install_adapters.py

# Web framework
fastapi>=0.136.0
# Note: uvicorn[standard] pulls in uvloop which does not support Windows.
# Use standard extras on non-Windows, and plain uvicorn on Windows.
uvicorn>=0.45.0; platform_system == "Windows"
uvicorn[standard]>=0.40.0; platform_system != "Windows"
python-multipart>=0.0.26

# Monitoring
prometheus-client>=0.25.0

# HTTP client for OIDC
httpx>=0.28.1

# JWT
PyJWT>=2.12.1
cryptography>=46.0.7

# Configuration and validation
pydantic>=2.13.3
pydantic-settings>=2.14.0

# For health checks in Docker
requests>=2.33.1

